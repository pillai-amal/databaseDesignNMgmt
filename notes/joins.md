Types of join:
## - Inner Join
    returns common elements in both tables
```sql
        SELECT column_names(s)
        FROM table1 
        INNER JOIN  table2 
        ON table1.attribute=table2.attribute;
``` 
       
## - Outer Join 

## - Cross Join
